import assert from'node:assert/strict';import{readFile}from'node:fs/promises';import test from'node:test';import*as plugin from'../dist/index.mjs';import{buildPluginArtifact}from'../tools/mgread.mjs';test('packages native Huya source deterministically',async()=>{assert.deepEqual(Object.keys(plugin).sort(),['activate','discover','getChapters','getContent','getDetail','search','searchSuggestions']);const p=JSON.parse(await readFile(new URL('../package.json',import.meta.url),'utf8'));assert.equal(p.mgread.id,'org.mgread.huya-live');const a=await buildPluginArtifact(),b=await buildPluginArtifact();assert.deepEqual(a.bytes,b.bytes);});
