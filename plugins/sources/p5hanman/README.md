# P5韩漫

MgRead Node 24 single-file conversion of repository source `p5hanman`.

The source uses Runtime-owned HTTP and resource proxy capabilities. Manga chapters return
ordered proxied pages with `text: null`; live HTML and image bodies are never saved as fixtures.
