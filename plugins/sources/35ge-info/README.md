# 35小说

MgRead Node.js 24 single-file conversion of repository source UUID
`019e120a-185b-7567-9c3d-3cb5e291e768` (`http://www.35ge.info`).

Only Runtime-owned HTTP and resource proxy capabilities are used. Local tests contain
sanitized deterministic HTML; live-network checks are kept in a separate test target.
