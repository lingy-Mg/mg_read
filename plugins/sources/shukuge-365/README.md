# 365小说网

MgRead Node.js 24 single-file conversion of repository source UUID
`019e120e-21b4-7377-8ec3-4d067a4350ac` (`http://www.shukuge.com`).

The plugin uses only the Runtime-owned HTTP and resource proxy boundaries. Tests use
sanitized local fixtures; real-network smoke stays separate in `test:live`.
