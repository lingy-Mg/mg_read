# 天悦小说

MgRead Node.js 24 single-file conversion of repository UUID
`019e1214-edbb-7f54-aea5-5678bd343263` (`https://www.xtyxsw.org`).

This conversion intentionally uses only currently verifiable public PC HTTP pages. It
does not copy the legacy browser fallback; a newly paginated/obfuscated chapter fails
explicitly instead of returning incomplete text.
