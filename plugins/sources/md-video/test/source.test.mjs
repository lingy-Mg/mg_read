import assert from'node:assert/strict';import test from'node:test';import*as plugin from'../dist/index.mjs';test('MD video source refetches stable line/index and proxies media',async()=>{const proxied=[],model={vod_id:12,vod_name:'测试',vod_play_from:'线路A',vod_play_url:'第一集$https://media.example/1.m3u8'};await plugin.activate({log:{info(){}},resource:{proxy(value){proxied.push(value);return'http://127.0.0.1/r';}},http:{async fetch(){return Response.json({code:1,page:1,pagecount:1,total:1,list:[model]});}}});const result=await plugin.discover({target:'channel:latest',cursor:null,collectionId:null,pageSize:3}),item=result.document.components[0].children[0].items[0].content,chapters=await plugin.getChapters({id:item.id}),content=await plugin.getContent({id:item.id,chapterId:chapters.items[0].id});assert.equal(chapters.items[0].id,'video:12:0:0');assert.equal(proxied.at(-1).url,'https://media.example/1.m3u8');assert.equal(content.media.resourceType,'hls');});
