import assert from'node:assert/strict';import test from'node:test';import*as plugin from'../dist/index.mjs';test('Juzi TV source signs requests, keeps ep IDs and proxies HLS',async()=>{const proxied=[];await plugin.activate({log:{info(){},warn(){}},resource:{proxy(v){proxied.push(v);return`http://127.0.0.1/r/${proxied.length}`;}},http:{async fetch(input){const url=String(input);if(url.includes('getVodList'))return Response.json({result:true,data:{items:[{vodId:10,vodName:'测试视频',coverImg:'https://img.example/c.jpg'}],totalPages:1}});if(url.includes('vodInfo/index'))return Response.json({result:true,data:{vodId:10,vodName:'测试视频',playerList:[{playerName:'线路',epList:[{epId:99,epName:'第一集'}]}]}});if(url.includes('epDetail'))return Response.json({result:true,data:[{vodResolution:3,canPlay:true}]});if(url.includes('playUrl'))return Response.json({result:true,data:{playUrl:'https://media.example/1.m3u8'}});return Response.json({result:true,data:{items:[]}});}}});const listing=await plugin.discover({target:'channel:short',cursor:null,collectionId:null,pageSize:5}),item=listing.document.components[0].children[0].items[0].content;assert.equal(item.id,'vod:10');const chapters=await plugin.getChapters({id:item.id});assert.equal(chapters.items[0].id,'vod:10:99');const content=await plugin.getContent({id:item.id,chapterId:chapters.items[0].id});assert.equal(content.media.resourceType,'video');assert.equal(proxied.at(-1).url,'https://media.example/1.m3u8');});
