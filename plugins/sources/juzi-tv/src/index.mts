/**
 * 橘子 TV 原生数据源。
 *
 * 职责：实现橘子 App MD5 动态签名，读取专题、检索、详情、分集与清晰度播放地址。
 * 生命周期：activate 生成会话内 udid；不写 Runtime 或系统安全存储。
 * IO：所有 App API 请求走 ctx.http；封面和视频只经 ctx.resource.proxy 输出。
 * 稳定标识：作品使用 vodId，章节使用 epId，不包含镜像域名、requestId 或签名。
 */
import{createHash,randomUUID}from'node:crypto';import type{MgReadPluginContext}from'@mgread/source-api';type Json=Record<string,unknown>;type Context=MgReadPluginContext;type Channel={id:string;title:string;topicId:number};type Episode={id:string;title:string;group:string};const hosts=['https://yz1018.ln2tn3kf2.com','https://yz260605.z5fl9630.com','https://yz260324.z2g1uoqy.com','https://yz260324.c628uthq.com','https://yz260324.nv153kfl.com','https://cfvip.eiq9rzoe.com','https://yz260605.jpknq5ju.com'],appKey='f384b87cc9ef41e4842dda977bae2c7f',common={appId:'fea23e11fc1241409682880e15fb2851',bundlerId:'com.murqanzze.ts',cus1tom:'cus3tom',deviceInfo:'NX809J',osInfo:'14',otherParam:'1',patchNumber:0,source:'wap_jztv3',version:'1.0.43',versionCode:1003},headers={'Content-Type':'application/json','User-Agent':'okhttp/4.12.0'},channels=Object.freeze<Channel[]>([{id:'short',title:'短剧',topicId:47},{id:'netflix',title:'奈飞 Netflix',topicId:5},{id:'movie',title:'电影',topicId:67},{id:'series',title:'电视剧',topicId:68},{id:'anime',title:'动漫',topicId:14},{id:'variety',title:'综艺',topicId:16},{id:'korea',title:'高清韩剧',topicId:63},{id:'sports',title:'体育',topicId:55}]);let context:Context|undefined,udid=randomUUID(),activeHost=hosts[0]??'';
export async function activate(next:Context){context=next;udid=randomUUID();activeHost=hosts[0]??'';next.log.info('source_activated');}
export async function search(request:{query:string;cursor:string|null;pageSize:number}){const query=request.query.trim();if(query==='')return frozen({items:[],nextCursor:null,totalCount:0});if(request.cursor!==null)throw new Error('Cursor is invalid.');const response=await post('/v1/api/search/search',{keyword:query,nextVal:''}),items=summaries(records(object(response.data).items)).slice(0,clamp(request.pageSize));return frozen({items,nextCursor:null,totalCount:items.length});}
export async function searchSuggestions(_request:{cursor:string|null;pageSize:number}){return frozen({items:[],nextCursor:null});}
export async function discover(request:{target:string|null;cursor:string|null;collectionId:string|null;pageSize:number}){if(request.target===null)return frozen({kind:'document'as const,document:{components:[{type:'section',id:'juzi-channels',title:'橘子 TV',subtitle:'影视专题',icon:'video',children:[{type:'categoryCollection',id:'juzi-channel-list',layout:'chips',categories:channels.map(channel=>({id:channel.id,title:channel.title,target:`channel:${channel.id}`,count:null,url:null,icon:'video'}))}]}]}});const channel=channels.find(value=>request.target===`channel:${value.id}`);if(!channel)throw new Error('Discovery target is invalid.');const page=cursorPage(request.cursor,request.target),size=clamp(request.pageSize),response=await post('/v1/api/vodTopic/getVodList',{vodTopicId:channel.topicId,pageIndex:page,pageSize:size}),data=object(response.data),values=records(first(data.items,data.vodList,data.list)),contents=summaries(values).slice(0,size),collectionId=`juzi:${channel.id}`,items=contents.map(content=>frozen({content,rank:null,metric:null,recommendation:null})),totalPages=Number(data.totalPages),continuation=(Number.isSafeInteger(totalPages)?page<totalPages:values.length>=size)?frozen({target:request.target,cursor:`channel:${channel.id}:${page+1}`}):null;if(request.collectionId!==null){if(request.collectionId!==collectionId)throw new Error('Discovery collection is invalid.');return frozen({kind:'append'as const,collectionId,items,continuation});}return frozen({kind:'document'as const,document:{components:[{type:'section',id:`${collectionId}:section`,title:channel.title,subtitle:null,icon:'video',children:[{type:'contentCollection',id:collectionId,layout:'coverGrid',items,continuation}]}]}});}
export async function getDetail(request:{id:string}){const id=contentId(request.id),data=object((await post('/v2/api/vodInfo/index',{vodId:Number(id)})).data),item=summary(data,id);return frozen({...item,aliases:[],catalogUrl:`juzi://vod/${id}`,chapterCount:episodes(data).length});}
export async function getChapters(request:{id:string}){const id=contentId(request.id),data=object((await post('/v2/api/vodInfo/index',{vodId:Number(id)})).data),values=episodes(data),items=values.map((episode,index)=>frozen({id:`vod:${id}:${episode.id}`,title:episode.title,order:index,url:null,volumeTitle:episode.group,wordCount:null,updatedAt:null,isLocked:false,attributes:[]})),groups=[...new Set(values.map(value=>value.group))].map((title,index)=>frozen({id:`group:${id}:${index}`,title,order:index,episodes:items.filter(item=>item.volumeTitle===title)}));return frozen({items,groups});}
export async function getContent(request:{id:string;chapterId:string}){const id=contentId(request.id),epId=chapterNative(request.chapterId,id),options=records((await post('/v2/api/vodInfo/epDetail',{vodEpId:Number(epId)})).data),resolutions=[...new Set([...options.filter(value=>value.canPlay!==false).map(value=>Number(value.vodResolution)),3,2,1].filter(value=>Number.isSafeInteger(value)&&value>0))];let upstream='';for(const resolution of resolutions){try{const data=object((await post('/v2/api/vodInfo/playUrl',{epId:Number(epId),vodResolution:resolution})).data);upstream=text(data.playUrl);if(safeUrl(upstream))break;}catch{continue;}}if(!safeUrl(upstream))throw new Error('Video address is unavailable.');const mediaHeaders={'User-Agent':'Mozilla/5.0',Referer:'https://55.app/'};return frozen({chapterId:request.chapterId,contentKind:'video',title:null,updatedAt:null,text:null,pages:[],media:{url:requireContext().resource.proxy({kind:'video',url:upstream,headers:mediaHeaders}),resourceType:'video',resourcePolicy:'sessionOnly',expiresAt:null,mimeType:/\.m3u8(?:$|[?#])/iu.test(upstream)?'application/vnd.apple.mpegurl':'video/mp4',headers:mediaHeaders}});}
async function post(path:string,extra:Readonly<Record<string,unknown>>){const body=sign({...common,...extra,udid,requestId:randomUUID()}),payload=JSON.stringify(body),ordered=[activeHost,...hosts.filter(host=>host!==activeHost)];let last='';for(const host of ordered){try{const response=await requireContext().http.fetch(`${host}${path}`,{method:'POST',headers,body:payload});if(!response.ok)throw new Error(`HTTP ${response.status}`);const value=object(JSON.parse(await response.text())as unknown);if(value.result){activeHost=host;return value;}throw new Error(text(value.msg)||'Upstream rejected request.');}catch(error){last=error instanceof Error?error.message:String(error);}}throw new Error(`Juzi request failed: ${last}`);}function sign(value:Record<string,unknown>){const canonical=Object.keys(value).filter(key=>key!=='sign'&&value[key]!==undefined&&value[key]!==null&&value[key]!=='').sort().map(key=>`${key}=${String(value[key])}`).concat(`appKey=${appKey}`).join('&');return{...value,sign:createHash('md5').update(canonical).digest('hex')};}
function summaries(values:Json[]){const result=new Map<string,ReturnType<typeof summary>>();for(const value of values){const id=sourceId(first(value.vodId,value.id));if(id&&text(first(value.vodName,value.name)))result.set(id,summary(value,id));}return[...result.values()];}function summary(value:Json,id:string){const latest=nullable(first(value.remark,value.updateRemark));return frozen({id:`vod:${id}`,title:text(first(value.vodName,value.name))||id,contentKind:'video',coverOrientation:'portrait',author:nullable(value.flags),url:`juzi://vod/${id}`,coverUrl:proxyImage(text(first(value.coverImg,value.coverUrl))),description:nullable(first(value.intro,value.watchingCountDesc)),language:'zh-CN',status:'unknown',access:'unknown',wordCount:null,chapterCount:null,publishedAt:null,updatedAt:null,latestChapter:latest?{id:`vod:${id}:latest`,title:latest,url:null,updatedAt:null}:null,categories:stringList([value.flags,value.areaName,value.year]),tags:[],attributes:[]});}function episodes(data:Json):Episode[]{const result:Episode[]=[];for(const[playerIndex,player]of records(data.playerList).entries()){const group=text(player.playerName)||`线路 ${playerIndex+1}`;for(const[episodeIndex,episode]of records(player.epList).entries()){const id=sourceId(episode.epId);if(id)result.push({id,title:text(episode.epName)||`第 ${episodeIndex+1} 集`,group});}}return result;}
function sourceId(value:unknown){const id=text(value);return /^\d+$/u.test(id)?id:null;}function contentId(id:string){const value=/^vod:(\d+)$/u.exec(id)?.[1];if(!value)throw new Error('Content ID is invalid.');return value;}function chapterNative(id:string,content:string){const value=new RegExp(`^vod:${content}:(\\d+)$`,'u').exec(id)?.[1];if(!value)throw new Error('Chapter ID is invalid.');return value;}function proxyImage(url:string){return safeUrl(url)?requireContext().resource.proxy({kind:'image',url,headers:{Referer:'https://55.app/'}}):null;}function safeUrl(value:string){try{return['http:','https:'].includes(new URL(value).protocol);}catch{return false;}}function cursorPage(cursor:string|null,target:string){if(cursor===null)return 1;const page=Number(cursor.startsWith(`${target}:`)?cursor.slice(target.length+1):'');if(!Number.isSafeInteger(page)||page<2)throw new Error('Cursor is invalid.');return page;}function stringList(values:unknown[]){return[...new Set(values.map(text).filter(Boolean))];}function records(value:unknown):Json[]{return Array.isArray(value)?value.filter(isObject):[];}function object(value:unknown):Json{return isObject(value)?value:{};}function isObject(value:unknown):value is Json{return value!==null&&typeof value==='object'&&!Array.isArray(value);}function first(...values:unknown[]){return values.find(value=>value!==null&&value!==undefined&&value!=='')??'';}function text(value:unknown){return typeof value==='string'?value.trim():typeof value==='number'?String(value):'';}function nullable(value:unknown){return text(value)||null;}function clamp(value:number){return Math.max(1,Math.min(50,Math.floor(value)));}function frozen<T>(value:T):T{return Object.freeze(value);}function requireContext(){if(!context)throw new Error('Source is not activated.');return context;}
