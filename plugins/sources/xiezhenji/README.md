# 写真集

MgRead standard source converted from repository UUID `xiezhenji` (`https://xx.knit.bid/`).
Current live evidence is Cloudflare 403, so protected HTML falls back to the versioned host-browser session capability.
