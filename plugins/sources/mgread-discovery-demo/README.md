# MgRead 发现组件演示数据源插件

离线内置数据源，用固定数据演示受控发现组件树、递归 group、多种内容集合、分类导航、返回栈和分类列表分页。它不访问网络，也不是实际内容来源。

`npm run verify` 默认生成确定性
`artifacts/org.mgread.discovery-demo-0.1.2.mgplugin.js`；artifact 只有规范信封与 ESM bundle，不含
lock、源码、`node_modules` 或 sidecar。
