import assert from'node:assert/strict';import test from'node:test';import*as plugin from'../dist/index.mjs';test('4KHD native pages follow pagination and proxy originals',async()=>{const resources=[],first='<a href="/content/15/test.html" title="测试图集"><img src="https://i1.wp.com/pic.4khd.com/cover.jpg"></a><p><a href="https://i1.wp.com/pic.4khd.com/1.jpg"><img src="x"></a></p><link rel="next" href="/content/15/test-2.html">',second='<p><a href="https://i2.wp.com/pic.4khd.com/2.jpg"><img src="x"></a></p>',detail='<title>测试图集 - 4KHD</title><meta property="og:image" content="https://i1.wp.com/pic.4khd.com/cover.jpg">';await plugin.activate({log:{info(){},warn(){}},resource:{proxy(v){resources.push(v);return`http://127.0.0.1/resource/${resources.length}`;}},http:{async fetch(input){const url=String(input);if(url.endsWith('test-2.html'))return new Response(second);if(url.includes('/content/15/test.html'))return new Response(url.includes('search')?first:first);if(url.includes('/search/'))return new Response(first);return new Response(detail);}}});const result=await plugin.search({query:'test',cursor:null,pageSize:5});const item=result.items[0];assert.ok(item.id.startsWith('manga:'));const chapters=await plugin.getChapters({id:item.id});const content=await plugin.getContent({id:item.id,chapterId:chapters.items[0].id});assert.equal(content.pages.length,2);assert.equal(resources.some(v=>v.url.endsWith('/2.jpg')),true);});
