/** Bz777 parser. Runtime browser provider owns Cookie, UA, challenge UI and session cache. */
import{Buffer}from'node:buffer';import*as cheerio from'cheerio/slim';import{PluginCache}from'@mgread/plugin-cache';
interface BrowserRequest{readonly version:1;readonly sessionKey:string;readonly url:string;readonly method:'GET'|'POST';readonly headers:Readonly<Record<string,string>>;readonly body:string|null;readonly interaction:'allow'|'silent';readonly presentation:'hidden'|'visible';readonly transport:'http'|'webview';readonly timeoutMs:number;readonly maxResponseBytes:number;}interface BrowserResponse{readonly status:number;readonly body:string;}export interface Context{readonly cacheDir:string;readonly browser:{readonly sessionV1:{request(request:BrowserRequest):Promise<BrowserResponse>}};readonly http:{fetch(input:string|URL,init?:RequestInit):Promise<Response>};readonly resource:{proxy(request:Record<string,unknown>):string};readonly log:{debug(v:string):void;info(v:string):void;warn(v:string):void;error(v:string):void};}
interface Summary{readonly id:string;readonly title:string;readonly contentKind:'novel';readonly author:string|null;readonly url:string;readonly coverUrl:string|null;readonly description:string|null;readonly language:'zh-CN';readonly status:'ongoing'|'completed'|'unknown';readonly access:'free';readonly wordCount:null;readonly chapterCount:number|null;readonly publishedAt:null;readonly updatedAt:null;readonly latestChapter:{readonly id:null;readonly title:string;readonly url:null;readonly updatedAt:null}|null;readonly categories:readonly string[];readonly tags:readonly string[];readonly attributes:readonly never[];}
const origin='https://www.bz777777777.com';const listing=Object.freeze({namespace:'listing',staleAfterMs:10*60*1000,serveStaleWhileRevalidate:true});const detail=Object.freeze({namespace:'detail',staleAfterMs:60*60*1000,allowStaleOnError:false});const catalog=Object.freeze({namespace:'catalog',staleAfterMs:60*60*1000,allowStaleOnError:false});export const categories=Object.freeze([['all','全部','0'],['fantasy','玄幻奇幻','1'],['martial','仙侠武侠','2'],['city','都市言情','3'],['history','穿越历史','4'],['scifi','科幻灵异','5'],['classic','藏经阁','6'],['other','其他类别','7']]as const);
export class BzSource{readonly#cache:PluginCache;constructor(private readonly context:Context){this.#cache=new PluginCache(context.cacheDir);}async search(query:string){const urls=[`/s.php?q=${encodeURIComponent(query)}`,`/search/?searchkey=${encodeURIComponent(query)}`,`/ss/?searchkey=${encodeURIComponent(query)}&submit=`];for(const path of urls){const url=new URL(path,origin);const html=await this.#browser(url,'GET',null);const items=this.parseList(html,url);if(items.length>0)return items;}const url=new URL('/s.php',origin);return this.parseList(await this.#browser(url,'POST',`objectType=2&type=articlename&s=${encodeURIComponent(query)}`),url);}async discover(id:string,page:number){const rule=categories.find(([key])=>key===id);if(rule===undefined)throw new Error('Unknown category.');const url=new URL(`/shuku/${rule[2]}-lastupdate-0-${page}.html`,origin);return this.parseList(await this.#cache.getOrFetchText(url,listing,()=>this.#browser(url,'GET',null)),url);}
parseList(html:string,base:URL):readonly Summary[]{const $=cheerio.load(html);const seen=new Set<string>();const items:Summary[]=[];$('.book-all-list li.column-2,.column-list ul.list li,.bookbox,.result-item,.book-img-text li,.rank-list li,.list-group-item,ul.searchlist li').each((_,element)=>{const root=$(element);const link=root.find('a.name,.name a,.right h4 a,.bookname a,h4 a,h3 a,a.book-name,.title a,a[href*="/book/"]').first();const href=link.attr('href');const title=clean(link.text());if(href===undefined||title===null)return;const url=new URL(href,base);if(!isBook(url)||seen.has(url.toString()))return;seen.add(url.toString());const image=root.find('img').first();const coverRaw=image.attr('data-src')??image.attr('data-original')??image.attr('src');const authorText=clean(root.find('a.author,.author,dt,.s4').first().text());const author=clean(/作者[：:]?\s*(.+)$/u.exec(authorText??'')?.[1])??authorText;const kind=clean(root.find('.cat,.s1').first().text());const latest=clean(root.find('p.update a,.update a,.s3 a').first().text());items.push(summary(url,title,author,coverRaw===undefined?null:this.#proxy(new URL(coverRaw,base),base),null,latest,kind===null?[]:[kind]));});return Object.freeze(items);}
async getDetail(id:string){const url=decode(id,'book');const html=await this.#cache.getOrFetchText(url,detail,()=>this.#browser(url,'GET',null));const $=cheerio.load(html);const title=clean($('meta[property="og:novel:book_name"]').attr('content'))??clean($('meta[property="og:title"]').attr('content'))??clean($('#info h1,h1.bookname,h1').first().text());if(title===null)throw new Error('Detail title is missing.');const info=clean($('#info,.bookinfo').first().text())??'';const author=clean($('meta[property="og:novel:author"]').attr('content'))??clean(/作者[：:]\s*([^\s<&]+)/u.exec(info)?.[1]);const coverRaw=clean($('meta[property="og:image"]').attr('content'))??clean($('#fmimg img,.bookimg img,.zhutu img').first().attr('src'));const intro=clean($('meta[property="og:description"]').attr('content'))??clean($('#intro,.intro,.bookinfo').first().text());const kind=clean($('meta[property="og:novel:category"]').attr('content'));const latest=clean($('meta[property="og:novel:latest_chapter_name"],meta[property="og:novel:lastest_chapter_name"]').first().attr('content'));return Object.freeze({...summary(url,title,author,coverRaw===null?null:this.#proxy(new URL(coverRaw,url),url),intro,latest,kind===null?[]:[kind]),aliases:Object.freeze([]),catalogUrl:url.toString()});}
async chapters(id:string){const first=decode(id,'book');const values:{id:string;title:string;order:number;url:string;volumeTitle:null;wordCount:null;updatedAt:null;isLocked:false;attributes:readonly never[]}[]=[];const seen=new Set<string>();let url=first;for(let page=0;page<60;page+=1){const html=await this.#cache.getOrFetchText(url,catalog,()=>this.#browser(url,'GET',null));const $=cheerio.load(html);const links=$('#list dd a,#chapterlist a,.chapterlist a,.chapter-list a,.listmain dd a,#catalog a,.directoryArea a').toArray();for(const element of links){const href=$(element).attr('href');const title=clean($(element).text());if(href===undefined||title===null)continue;const chapter=new URL(href,url);if(!isChapter(chapter)||seen.has(chapter.toString()))continue;seen.add(chapter.toString());values.push({id:`chapter:${token(chapter)}`,title,order:values.length,url:chapter.toString(),volumeTitle:null,wordCount:null,updatedAt:null,isLocked:false,attributes:Object.freeze([])});}const next=$('a').filter((_,element)=>clean($(element).text())==='下一页').first().attr('href');if(next===undefined)break;const candidate=new URL(next,url);if(candidate.origin!==origin||candidate.toString()===url.toString())break;url=candidate;}if(values.length===0)throw new Error('Catalog is empty.');return Object.freeze({items:Object.freeze(values)});}
async content(id:string,chapterId:string){decode(id,'book');let url=decode(chapterId,'chapter');const lines:string[]=[];const seen=new Set<string>();for(let page=0;page<15&&!seen.has(url.toString());page+=1){seen.add(url.toString());const html=await this.#browser(url,'GET',null);const $=cheerio.load(html);const root=$('.page-content,#chaptercontent,#content,.read-content,#chapterText,.chapter-content').first();for(const paragraph of root.find('p').length>0?root.find('p').toArray():[root.get(0)].filter(Boolean)){const line=clean($(paragraph!).text());if(line!==null&&!/(?:本章未完|加入书签|章节报错)/u.test(line))lines.push(line);}const next=$('a').filter((_,element)=>clean($(element).text())==='下一页').first().attr('href');if(next===undefined)break;const candidate=new URL(next,url);if(candidate.origin!==origin||candidate.toString()===url.toString())break;url=candidate;}const text=lines.join('\n\n');if(text==='')throw new Error('Chapter text is empty.');return Object.freeze({chapterId,contentKind:'novel'as const,title:null,updatedAt:null,text,pages:Object.freeze([])});}
async resource(request:Record<string,unknown>){if(request.kind!=='image'||typeof request.url!=='string'||typeof request.referer!=='string')return empty(400);const url=new URL(request.url),referer=new URL(request.referer);if(url.origin!==origin||referer.origin!==origin)return empty(400);const response=await this.context.http.fetch(url,{headers:{accept:'image/*',referer:referer.toString()}});const body=new Uint8Array(await response.arrayBuffer());const type=response.headers.get('content-type');return Object.freeze({status:response.status,headers:type===null?{}:{'content-type':type},body});}async#browser(url:URL,method:'GET'|'POST',body:string|null){const response=await this.context.browser.sessionV1.request({version:1,sessionKey:'bz777777777',url:url.toString(),method,headers:method==='POST'?{'content-type':'application/x-www-form-urlencoded',accept:'text/html'}:{accept:'text/html'},body,interaction:'allow',presentation:'visible',transport:'webview',timeoutMs:120000,maxResponseBytes:2*1024*1024});if(response.status>=400||/(?:cf-challenge|cf-turnstile|Just a moment|Checking your browser)/iu.test(response.body))throw new Error('Browser verification is incomplete.');return response.body;}#proxy(url:URL,referer:URL){return this.context.resource.proxy({kind:'image',url:url.toString(),referer:referer.toString()});}}
function summary(url:URL,title:string,author:string|null,coverUrl:string|null,description:string|null,latest:string|null,categories:readonly string[]):Summary{return Object.freeze({id:`book:${token(url)}`,title:title.replace(/^\s*\[[^\]]+\]\s*/u,''),contentKind:'novel',author,url:url.toString(),coverUrl,description,language:'zh-CN',status:'unknown',access:'free',wordCount:null,chapterCount:null,publishedAt:null,updatedAt:null,latestChapter:latest===null?null:{id:null,title:latest,url:null,updatedAt:null},categories:Object.freeze(categories),tags:Object.freeze([]),attributes:Object.freeze([])});}function token(url:URL){return Buffer.from(`${url.pathname}${url.search}`,'utf8').toString('base64url');}function decode(value:string,prefix:'book'|'chapter'){const match=new RegExp(`^${prefix}:([A-Za-z0-9_-]+)$`,'u').exec(value);if(match?.[1]===undefined)throw new Error('Opaque ID is invalid.');const url=new URL(Buffer.from(match[1],'base64url').toString('utf8'),origin);if(prefix==='book'?!isBook(url):!isChapter(url))throw new Error('Opaque ID is invalid.');return url;}function isBook(url:URL){return url.origin===origin&&(/\/book\/\d+\/?$/u.test(url.pathname)||/\/\d+\/\d+\/?$/u.test(url.pathname));}function isChapter(url:URL){return url.origin===origin&&(/\/book\/\d+\/\d+\.html$/u.test(url.pathname)||/\/(?:xs|look)\/\d+\/\d+\.html$/u.test(url.pathname));}function clean(value:string|undefined){const result=value?.replace(/\s+/gu,' ').trim()??'';return result===''?null:result;}function empty(status:number){return Object.freeze({status,headers:Object.freeze({}),body:new Uint8Array()});}
