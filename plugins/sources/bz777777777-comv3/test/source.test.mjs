import assert from'node:assert/strict';import test from'node:test';import{mkdtemp,readFile,rm}from'node:fs/promises';import{tmpdir}from'node:os';import{join}from'node:path';import*as plugin from'../dist/index.mjs';test('browser fixture covers search detail full catalog text cover proxy and no credential fields',async t=>{const cacheDir=await mkdtemp(join(tmpdir(),'bz-cache-'));t.after(()=>rm(cacheDir,{recursive:true,force:true}));const list=await readFile(new URL('./fixtures/list.html',import.meta.url),'utf8'),detail=await readFile(new URL('./fixtures/detail.html',import.meta.url),'utf8'),content=await readFile(new URL('./fixtures/content.html',import.meta.url),'utf8');const calls=[],resources=[];await plugin.activate({dataDir:cacheDir,cacheDir,app:{},plugin:{},log:{debug(){},info(){},warn(){},error(){}},resource:{proxy(r){resources.push(r);return`http://127.0.0.1/r/${resources.length}`;}},http:{async fetch(){return new Response(new Uint8Array([9]))}},browser:{sessionV1:{async request(r){calls.push(r);const path=new URL(r.url).pathname;return{status:200,body:path.endsWith('.html')?content:path==='/book/123/'?detail:list};}}}});const search=await plugin.search({query:'fixture',cursor:null,pageSize:20});assert.equal(search.items[0].title,'Fixture Novel');const info=await plugin.getDetail({id:search.items[0].id});assert.ok(info.coverUrl);const chapters=await plugin.getChapters({id:info.id});assert.equal(chapters.items.length,2);const body=await plugin.getContent({id:info.id,chapterId:chapters.items[0].id});assert.match(body.text,/Fixture text/u);assert.ok(calls.every(r=>r.headers.cookie===undefined&&r.headers['user-agent']===undefined&&r.transport==='webview'&&r.presentation==='visible'));});
