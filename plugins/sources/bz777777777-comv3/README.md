# 第一版主(7777)v3

MgRead conversion of repository UUID `bz777777777-comv3` (`https://www.bz777777777.com`).
Protected HTML uses host-owned `browser.session.v1`; temporary Cookie or UA values are never embedded.
