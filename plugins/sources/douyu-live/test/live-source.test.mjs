import assert from'node:assert/strict';import test from'node:test';import*as plugin from'../dist/index.mjs';test('live Douyu categories and rooms remain reachable',{timeout:60000},async()=>{await plugin.activate({log:{info(){},warn(){}},resource:{proxy(){return'http://127.0.0.1/live-resource';}},http:{fetch:(input,init={})=>fetch(input,{...init,signal:AbortSignal.timeout(15000)})}});const root=await plugin.discover({target:null,cursor:null,collectionId:null,pageSize:3});const category=root.document.components[0].children[0].categories[0];const result=await plugin.discover({target:category.target,cursor:null,collectionId:null,pageSize:3});assert.ok(result.document.components[0].children[0].items.length>0);});
