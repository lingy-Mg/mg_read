import assert from'node:assert/strict';import test from'node:test';import*as plugin from'../dist/index.mjs';test('live Deqi catalog remains reachable',{timeout:60000},async()=>{await plugin.activate({log:{info(){},warn(){}},resource:{proxy(){return'http://127.0.0.1/r';}},http:{fetch:(input,init={})=>fetch(input,{...init,signal:AbortSignal.timeout(20000)})}});const charts=await plugin.discover({target:null,cursor:null,collectionId:null,pageSize:20});assert.equal(charts.document.components[1].children[0].categories.length,35);const ranking=await plugin.discover({target:'chart:allvisit',cursor:null,collectionId:null,pageSize:3});assert.ok(ranking.document.components[0].children[0].items.length>0);const result=await plugin.discover({target:'channel:1',cursor:null,collectionId:null,pageSize:3});assert.ok(result.document.components[0].children[0].items.length>0);});
