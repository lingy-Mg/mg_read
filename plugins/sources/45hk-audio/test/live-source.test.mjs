import assert from'node:assert/strict';import test from'node:test';import*as plugin from'../dist/index.mjs';test('live 45hk chart remains reachable or reports verification',{timeout:60000},async()=>{await plugin.activate({log:{info(){},warn(){}},resource:{proxy(){return'http://127.0.0.1/live-resource';}},http:{fetch:(input,init={})=>fetch(input,{...init,signal:AbortSignal.timeout(15000)})}});const r=await plugin.discover({target:'category:top',cursor:null,collectionId:null,pageSize:3});assert.ok(r.document.components[0].children[0].items.length>0);});
