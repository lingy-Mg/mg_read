import assert from'node:assert/strict';import test from'node:test';import*as plugin from'../dist/index.mjs';test('live Cupfox search remains reachable',{timeout:60000},async()=>{await plugin.activate({log:{info(){},warn(){}},resource:{proxy(){return'http://127.0.0.1/live-resource';}},http:{fetch:(input,init={})=>fetch(input,{...init,signal:AbortSignal.timeout(15000)})}});const result=await plugin.search({query:'斗破苍穹',cursor:null,pageSize:3});assert.ok(Array.isArray(result.items));});
