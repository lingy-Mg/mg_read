import assert from'node:assert/strict';import test from'node:test';import*as plugin from'../dist/index.mjs';test('Eev3 native flow handles verification and proxies audio',async()=>{const resources=[],calls=[],list='<div class="play_list"><ul><li><a href="/mp3/songabc.html" title="周深《测试歌曲》">歌曲</a></li></ul></div>',detail='<title>周深《测试歌曲》 - 易听音乐网</title><img id="mcover" src="/cover.jpg"><div class="sm">所属专辑：测试</div>';let verified=false;await plugin.activate({log:{info(){},warn(){}},resource:{proxy(v){resources.push(v);return`http://127.0.0.1/resource/${resources.length}`;}},http:{async fetch(input,init={}){const url=String(input);calls.push({url,init});if(url.endsWith('/js/play.php'))return Response.json({url:'https://media.example/song.mp3'});if(init.method==='POST'){verified=true;return new Response(detail,{headers:{'set-cookie':'verified=1; Path=/'}});}if(url.includes('/mp3/')&&!verified)return new Response('<form>安全人机验证<input name="csrf_token" value="token-a"><input name="human_check"></form>');return new Response(url.includes('/mp3/')?detail:list);}}});const root=await plugin.discover({target:'category:top',cursor:null,collectionId:null,pageSize:5});const id=root.document.components[0].children[0].items[0].content.id;const info=await plugin.getDetail({id});assert.equal(info.author,'周深');const chapters=await plugin.getChapters({id});const content=await plugin.getContent({id,chapterId:chapters.items[0].id});assert.equal(content.media.resourceType,'audio');assert.equal(resources.find(v=>v.kind==='audio').url,'https://media.example/song.mp3');assert.ok(calls.some(call=>call.init.method==='POST'&&String(call.init.body).includes('csrf_token')));});
